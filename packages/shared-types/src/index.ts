/** Supported interface languages (English at the root, Arabic under /ar). */
export type Locale = 'en' | 'ar';

/** Text direction; Arabic is right-to-left. */
export type TextDirection = 'ltr' | 'rtl';
