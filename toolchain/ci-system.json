{
  "purpose": "CI system expectations (owner decisions E6, E9, E10, E23).",
  "runner": "ubuntu-24.04",
  "runnerNote": "GitHub-hosted runner images are updated by GitHub and are not digest-pinned; tool versions below are verified in every run instead.",
  "node": "24.21.0",
  "pnpm": "12.4.2",
  "redis": {
    "source": "Ubuntu 24.04 apt package redis-server",
    "expectedVersion": "7.0.15"
  },
  "psql": {
    "source": "Ubuntu 24.04 apt package postgresql-client-16",
    "expectedMajor": "16",
    "note": "Client only, for the b10-hosted job: it applies SQL and runs verification queries. No pg_dump or pg_restore, so a 16 client against the hosted PostgreSQL 17 server is sufficient and no PGDG package source is introduced (owner decision, 2026-09-21). Patch versions follow the Ubuntu archive, so the major is what is pinned."
  },
  "playwright": {
    "package": "@playwright/test 1.62.1",
    "browser": "chromium",
    "browserTitle": "Chrome for Testing",
    "browserVersion": "151.0.7922.34",
    "revision": "1234",
    "install": "pnpm --filter @repo/e2e exec playwright install --with-deps chromium"
  }
}
